import React from "react";

const Hero = () => {
  return (
    <div className="relative w-full">
      {/* Cloud SVG at bottom */}
      <img src="/svg/cloud.svg" alt="clouds" className="w-full" />
    </div>
  );
};

export default Hero;
